# Models

This directory is reserved for portable, top-level model exports and deployment-ready bundles.

The active pipeline contract in this repository stores generated artifacts under `processed/models/`.
When a model is promoted for deployment, it can be copied or exported here without changing the training pipelines.
