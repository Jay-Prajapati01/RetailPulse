# Notebook Layer

This folder is reserved for notebook entrypoints that stay thin and call reusable code under `src/`.

Current notebooks live at the repository root for continuity with prior stages. New notebooks should be added here once the older ones are consolidated.
