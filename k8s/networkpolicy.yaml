apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: retailpulse-default
  namespace: retailpulse
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector: {}
